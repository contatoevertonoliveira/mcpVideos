# infra/nginx

Reverse proxy / TLS termination para staging e produção. Não necessário para desenvolvimento local (Fase 01 expõe `web` e `api` diretamente via `docker-compose.yml`). Adicionar configuração quando o ambiente de deployment for definido.
