# packages/shared

Código utilitário compartilhado entre `apps/web` e `apps/api` (ou entre múltiplos serviços TypeScript) quando essa necessidade surgir. Sem conteúdo na Fase 01 — evitar abstração prematura (Documento 02, seção 84).
