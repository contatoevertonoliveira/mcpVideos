# tests

Testes unitários e de integração vivem junto de cada app (`apps/api/tests`, `apps/web/src/**/*.test.ts`). Este diretório é reservado para testes **E2E cross-stack** (Documento 10, seção 50 — login, connect channel, onboarding, etc.), que só fazem sentido a partir da Fase 03 (Authentication) em diante. Sem conteúdo na Fase 01.
